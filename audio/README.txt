DROP YOUR MUSIC HERE
====================
Save two looping MP3s with these EXACT names:

  light.mp3  -> plays in the normal (red/gold) site
  elmo.mp3   -> plays in Elmo mode (right-click the profile photo)

Notes:
- Both loop automatically and crossfade (~0.7s) when you switch modes.
- The volume slider (bottom-right) controls both.
- Music starts on your first click/keypress anywhere on the page
  (browsers block auto-playing sound before any interaction).
- If a file is missing, nothing breaks - that track just stays silent.
